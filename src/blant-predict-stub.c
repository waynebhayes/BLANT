#include "misc.h"
#include "blant.h"
#include "tinygraph.h"
#include "graph.h"

void Predict_Init(GRAPH *G){Fatal("prediction not supported");}
void Predict_Flush(GRAPH *G){Fatal("prediction not supported");};
void Predict_ProcessLine(GRAPH *G, char line[]){Fatal("prediction not supported");};
Gordinal_type L_K_Func(Gint_type Gint) {Apology("prediction not supported"); return -1;}
int  Predict_Merge(GRAPH *G){Fatal("prediction not supported"); return -1;}
Gordinal_type canon_to_ordinal(Gint_type canon, int k){Fatal("prediction not supported"); return -1;}
void Predict_ProcessGraphlet(GRAPH *G, unsigned Varray[], TINY_GRAPH *g, Gint_type Gint, Gordinal_type GintOrdinal)
    {Fatal("prediction not supported");};
Gint_type smaller_canon_map(Gint_type num, int k, unsigned char* return_permutation)
    {Fatal("prediction not supported"); return -1;}
