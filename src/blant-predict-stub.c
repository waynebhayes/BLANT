#include "misc.h"
#include "blant.h"
#include "tinygraph.h"
#include "graph.h"

void Predict_Init(GRAPH *G){Fatal("prediction not supported");}
void Predict_FlushMotifs(GRAPH *G){Fatal("prediction not supported");};
void Predict_ProcessLine(GRAPH *G, char line[]){Fatal("prediction not supported");};
unsigned int L_K_Func(Gint_type Gint) {Apology("prediction not supported"); return -1;}

void Predict_AccumulateMotifs(GRAPH *G, unsigned Varray[], TINY_GRAPH *g, Gint_type Gint, Gordinal_type GintOrdinal){Fatal("prediction not supported by your version of gcc");};
int  Predict_Merge(GRAPH *G){Fatal("prediction not supported by your version of gcc"); return -1;}

